{
  "name": "ui-web-framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:prod": "ENV=prod playwright test",
    "test:prod:smoke": "ENV=prod playwright test --grep @smoke",
    "test:run": "ENV=${ENV:-prod} playwright test ${BROWSER:+--project=$BROWSER} ${SUITE:+--grep @$SUITE}",
    "test:debug": "ENV=${ENV:-prod} playwright test ${BROWSER:+--project=$BROWSER} --debug ${SUITE:+--grep @$SUITE}",
    "report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.5.0"
  }
}
